# 리눅스에서 D드라이브로 변경하기

cmd를 사용할 때는 `cd D:`로 현재 위치를 D드라이브로 이동했었는데, 리눅스는 해당 커맨드가 먹히지 않았다.
찾아보니 리눅스에는 디스크 장치를 디렉터리에 연결시켜주는 **마운트(mount)**라는 것이 있다.
그래서 마운트 경로를 포함해서 change directory를 하니 잘 변경되었다.

```
cd /mnt/d
```